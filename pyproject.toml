[project]
name = "psysuccess-ai-pisa2022"
version = "0.1.0"
description = "Validity-gated psychometric machine learning for student success prediction using PISA 2022."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
